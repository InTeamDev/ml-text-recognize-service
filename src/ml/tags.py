def get(source: str) -> list[str]:
    """Получение ключевых слов (тэгов) из текста

    Args:
        source (str): исходный текст

    Returns:
        list[str]: теги
    """

    # some logic

    return source.split()
